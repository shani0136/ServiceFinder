import type { Provider } from '../types';

/**
 * Empty provider list. Fake/fabricated providers are strictly prohibited.
 * All providers in ServiceFinder must come from real registrations and administrator approvals.
 */
export const DEMO_PROVIDERS: Provider[] = [];
